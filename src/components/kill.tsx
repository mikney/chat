import React from 'react';
import { Button } from 'antd';

const Kill = () => {

    const num: number = 33
    return (
        <div className={'h'}>
            {num}
            hello
            syka
            <Button/>
            <Button type="primary" >H</Button>
            davai
        </div>
    );
};

export default Kill;